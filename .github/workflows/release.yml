# Release / publish workflow
#
# Version-driven release cycle:
#   1. A developer sets the root VERSION file to a clean semver (e.g. "1.2.0"),
#      removes the "-dev" suffix, and merges to main.
#   2. Job A (publish) detects the clean version, builds and pushes the
#      session-broker, config-broker, control-plane, db-migrate, and bootstrap
#      containers to GHCR, builds release binaries for botwork-launcher and
#      botwork-tools, and creates a GitHub Release v<VERSION> with those
#      binaries attached.
#   3. Job B (bump) triggers on the published Release event, increments the
#      minor version, appends "-dev", and commits the new VERSION back to main
#      (e.g. "1.2.0" → "1.3.0-dev").
#   4. That "-dev" push to main is a no-op for Job A, completing the loop.
#
# How to cut a release:
#   - Edit VERSION (repo root) to a clean semver, e.g. "1.2.0"
#   - Open a PR, get it merged to main
#   - Automation handles everything else

name: Release

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:

# Default to least-privilege; individual jobs override as needed.
permissions:
  contents: read

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # Job A — publish container + binaries + GitHub Release
  # Runs on every push to main, but exits immediately when VERSION ends in
  # "-dev" (development snapshots are never published).
  # ─────────────────────────────────────────────────────────────────────────
  publish:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write   # create tags and releases, upload assets
      packages: write   # push images to GHCR

    steps:
    - uses: actions/checkout@v4

    # ── Read and validate the VERSION file ──────────────────────────────────
    - name: Read VERSION
      id: version
      run: |
        set -euo pipefail
        raw="$(tr -d '[:space:]' < VERSION)"
        echo "version=${raw}" >> "$GITHUB_OUTPUT"
        # Exit cleanly (skip publish) when the version contains a hyphen
        # (i.e. any pre-release/dev suffix such as "-dev", "-beta1", "-rc2").
        # Only a bare "MAJOR.MINOR.PATCH" triggers a publish so that automated
        # bump commits never cause a double-release.
        if [[ "${raw}" == *-* ]]; then
          echo "is_release=false" >> "$GITHUB_OUTPUT"
          echo "VERSION=${raw} contains a pre-release suffix — skipping publish."
        else
          echo "is_release=true" >> "$GITHUB_OUTPUT"
          echo "VERSION=${raw} is a clean semver — proceeding with publish."
        fi

    # ── Guard: all subsequent steps are skipped for -dev versions ───────────

    # ── Container: log in to GHCR ───────────────────────────────────────────
    - name: Log in to GHCR
      if: steps.version.outputs.is_release == 'true'
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Install EarthBuild
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        tmp="$(mktemp -d)"
        base="https://github.com/EarthBuild/earthbuild/releases/download/v0.8.17"
        curl -fsSL -o "${tmp}/earth-linux-amd64" "${base}/earth-linux-amd64"
        curl -fsSL -o "${tmp}/checksum.asc" "${base}/checksum.asc"
        ( cd "${tmp}" && grep ' earth-linux-amd64$' checksum.asc | sha256sum -c - )
        sudo install -m 0755 "${tmp}/earth-linux-amd64" /usr/local/bin/earthly

    # ── Container: build and push session-broker ────────────────────────────
    - name: Build and push session-broker container
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.version }}"
        REPO="ghcr.io/botworkz/botwork/session-broker"
        # Platform is pinned in Earthfile via FROM DOCKERFILE --platform=linux/amd64.
        earthly --no-cache --build-arg GIT_SHA="${GITHUB_SHA}" +session-broker-image
        expected="${GITHUB_SHA}"
        actual="$(docker image inspect botwork/session-broker:local \
          --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')"
        if [[ "${actual}" != "${expected}" ]]; then
          echo "ERROR: built image has revision label '${actual}' but" \
               "workflow was triggered by commit '${expected}'." >&2
          echo "Refusing to push a release container whose provenance does not match the triggering commit." >&2
          echo "Also dumping all labels on the built image for diagnosis:" >&2
          docker image inspect botwork/session-broker:local --format '{{ json .Config.Labels }}' >&2
          exit 1
        fi
        echo "verified: image revision label matches GITHUB_SHA (${expected})"
        docker tag botwork/session-broker:local "${REPO}:${VERSION}"
        docker tag botwork/session-broker:local "${REPO}:latest"
        docker push "${REPO}:${VERSION}"
        docker push "${REPO}:latest"

    # ── Container: build and push config-broker ─────────────────────────────
    - name: Build and push config-broker container
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.version }}"
        REPO="ghcr.io/botworkz/botwork/config-broker"
        # Platform is pinned in Earthfile via FROM DOCKERFILE --platform=linux/amd64.
        earthly --no-cache --build-arg GIT_SHA="${GITHUB_SHA}" +config-broker-image
        expected="${GITHUB_SHA}"
        actual="$(docker image inspect botwork/config-broker:local \
          --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')"
        if [[ "${actual}" != "${expected}" ]]; then
          echo "ERROR: built image has revision label '${actual}' but" \
               "workflow was triggered by commit '${expected}'." >&2
          echo "Refusing to push a release container whose provenance does not match the triggering commit." >&2
          echo "Also dumping all labels on the built image for diagnosis:" >&2
          docker image inspect botwork/config-broker:local --format '{{ json .Config.Labels }}' >&2
          exit 1
        fi
        echo "verified: image revision label matches GITHUB_SHA (${expected})"
        docker tag botwork/config-broker:local "${REPO}:${VERSION}"
        docker tag botwork/config-broker:local "${REPO}:latest"
        docker push "${REPO}:${VERSION}"
        docker push "${REPO}:latest"

    # ── Container: build and push control-plane ─────────────────────────────
    # Symmetric to session-broker/config-broker: build the image with the
    # triggering commit baked into the revision label, refuse to push if
    # the label does not match GITHUB_SHA (catches any future ARG/LABEL
    # regression in the Dockerfile), then tag :VERSION + :latest.
    #
    # The image landed in the codebase via PR #82 but was not added to
    # the publish pipeline at the time; this step closes that gap so the
    # vm side can shasset-pin against control-plane in lock-step with
    # session-broker and config-broker.
    - name: Build and push control-plane container
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.version }}"
        REPO="ghcr.io/botworkz/botwork/control-plane"
        # Platform is pinned in Earthfile via FROM DOCKERFILE --platform=linux/amd64.
        earthly --no-cache --build-arg GIT_SHA="${GITHUB_SHA}" +control-plane-image
        expected="${GITHUB_SHA}"
        actual="$(docker image inspect botwork/control-plane:local \
          --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')"
        if [[ "${actual}" != "${expected}" ]]; then
          echo "ERROR: built image has revision label '${actual}' but" \
               "workflow was triggered by commit '${expected}'." >&2
          echo "Refusing to push a release container whose provenance does not match the triggering commit." >&2
          echo "Also dumping all labels on the built image for diagnosis:" >&2
          docker image inspect botwork/control-plane:local --format '{{ json .Config.Labels }}' >&2
          exit 1
        fi
        echo "verified: image revision label matches GITHUB_SHA (${expected})"
        docker tag botwork/control-plane:local "${REPO}:${VERSION}"
        docker tag botwork/control-plane:local "${REPO}:latest"
        docker push "${REPO}:${VERSION}"
        docker push "${REPO}:latest"

    # ── Container: build and push db-migrate ────────────────────────────────
    # Symmetric to the other broker images. db-migrate is a oneshot, not a
    # server, but the publish contract is identical: build with revision
    # label, refuse to push on mismatch, tag :VERSION + :latest. vm-side
    # shasset-pins against this image just like the others.
    - name: Build and push db-migrate container
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.version }}"
        REPO="ghcr.io/botworkz/botwork/db-migrate"
        # Platform is pinned in Earthfile via FROM DOCKERFILE --platform=linux/amd64.
        earthly --no-cache --build-arg GIT_SHA="${GITHUB_SHA}" +db-migrate-image
        expected="${GITHUB_SHA}"
        actual="$(docker image inspect botwork/db-migrate:local \
          --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')"
        if [[ "${actual}" != "${expected}" ]]; then
          echo "ERROR: built image has revision label '${actual}' but" \
               "workflow was triggered by commit '${expected}'." >&2
          echo "Refusing to push a release container whose provenance does not match the triggering commit." >&2
          echo "Also dumping all labels on the built image for diagnosis:" >&2
          docker image inspect botwork/db-migrate:local --format '{{ json .Config.Labels }}' >&2
          exit 1
        fi
        echo "verified: image revision label matches GITHUB_SHA (${expected})"
        docker tag botwork/db-migrate:local "${REPO}:${VERSION}"
        docker tag botwork/db-migrate:local "${REPO}:latest"
        docker push "${REPO}:${VERSION}"
        docker push "${REPO}:latest"

    # ── Container: build and push bootstrap ─────────────────────────────────
    # Symmetric to db-migrate (also a oneshot). Same build/verify/tag/push
    # contract so vm-side shasset can pin this image alongside the others.
    # The systemd unit botwork-bootstrap.service runs this between
    # botwork-db-migrate.service (schema landed) and
    # botwork-config-broker.service (will read from the DB post-cutover).
    - name: Build and push bootstrap container
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.version }}"
        REPO="ghcr.io/botworkz/botwork/bootstrap"
        # Platform is pinned in Earthfile via FROM DOCKERFILE --platform=linux/amd64.
        earthly --no-cache --build-arg GIT_SHA="${GITHUB_SHA}" +bootstrap-image
        expected="${GITHUB_SHA}"
        actual="$(docker image inspect botwork/bootstrap:local \
          --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')"
        if [[ "${actual}" != "${expected}" ]]; then
          echo "ERROR: built image has revision label '${actual}' but" \
               "workflow was triggered by commit '${expected}'." >&2
          echo "Refusing to push a release container whose provenance does not match the triggering commit." >&2
          echo "Also dumping all labels on the built image for diagnosis:" >&2
          docker image inspect botwork/bootstrap:local --format '{{ json .Config.Labels }}' >&2
          exit 1
        fi
        echo "verified: image revision label matches GITHUB_SHA (${expected})"
        docker tag botwork/bootstrap:local "${REPO}:${VERSION}"
        docker tag botwork/bootstrap:local "${REPO}:latest"
        docker push "${REPO}:${VERSION}"
        docker push "${REPO}:latest"

    # ── Container: build and push admin-api ─────────────────────────────────
    # Symmetric to config-broker. admin-api is an HTTP+JSON server that
    # wraps the entity layer (RFE #106). v0 ships only a health endpoint;
    # the entity CRUD handlers land in PR2. Same build/verify/tag/push
    # contract so vm-side shasset can pin this image alongside the others.
    - name: Build and push admin-api container
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.version }}"
        REPO="ghcr.io/botworkz/botwork/admin-api"
        # Platform is pinned in Earthfile via FROM DOCKERFILE --platform=linux/amd64.
        earthly --no-cache --build-arg GIT_SHA="${GITHUB_SHA}" +admin-api-image
        expected="${GITHUB_SHA}"
        actual="$(docker image inspect botwork/admin-api:local \
          --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')"
        if [[ "${actual}" != "${expected}" ]]; then
          echo "ERROR: built image has revision label '${actual}' but" \
               "workflow was triggered by commit '${expected}'." >&2
          echo "Refusing to push a release container whose provenance does not match the triggering commit." >&2
          echo "Also dumping all labels on the built image for diagnosis:" >&2
          docker image inspect botwork/admin-api:local --format '{{ json .Config.Labels }}' >&2
          exit 1
        fi
        echo "verified: image revision label matches GITHUB_SHA (${expected})"
        docker tag botwork/admin-api:local "${REPO}:${VERSION}"
        docker tag botwork/admin-api:local "${REPO}:latest"
        docker push "${REPO}:${VERSION}"
        docker push "${REPO}:latest"

    # ── Container: build and push admin-ui ──────────────────────────────────
    # Symmetric to admin-api. admin-ui is the operator-facing Leptos panel
    # (RFE #106 follow-up). v0 ships a one-page health-probe shell; entity
    # CRUD views land alongside the admin-api PR2 work. The image is the
    # only botwork image whose build runs `trunk build --release` before
    # `cargo build`; that wrinkle is entirely inside the Dockerfile so the
    # publish contract here stays the same.
    - name: Build and push admin-ui container
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.version }}"
        REPO="ghcr.io/botworkz/botwork/admin-ui"
        # Platform is pinned in Earthfile via FROM DOCKERFILE --platform=linux/amd64.
        earthly --no-cache --build-arg GIT_SHA="${GITHUB_SHA}" +admin-ui-image
        expected="${GITHUB_SHA}"
        actual="$(docker image inspect botwork/admin-ui:local \
          --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}')"
        if [[ "${actual}" != "${expected}" ]]; then
          echo "ERROR: built image has revision label '${actual}' but" \
               "workflow was triggered by commit '${expected}'." >&2
          echo "Refusing to push a release container whose provenance does not match the triggering commit." >&2
          echo "Also dumping all labels on the built image for diagnosis:" >&2
          docker image inspect botwork/admin-ui:local --format '{{ json .Config.Labels }}' >&2
          exit 1
        fi
        echo "verified: image revision label matches GITHUB_SHA (${expected})"
        docker tag botwork/admin-ui:local "${REPO}:${VERSION}"
        docker tag botwork/admin-ui:local "${REPO}:latest"
        docker push "${REPO}:${VERSION}"
        docker push "${REPO}:latest"

    # ── Binaries: build botwork-launcher and botwork-tools ──────────────────
    - uses: dtolnay/rust-toolchain@stable
      if: steps.version.outputs.is_release == 'true'
    - uses: Swatinem/rust-cache@v2
      if: steps.version.outputs.is_release == 'true'

    - name: Build release binaries
      if: steps.version.outputs.is_release == 'true'
      run: |
        set -euo pipefail
        cargo build --release -p botwork-launcher -p botwork-tools

    # ── GitHub Release: create and upload assets ─────────────────────────────
    # Uses softprops/action-gh-release which creates the tag and release in
    # one step. If the tag already exists the action will fail with a clear
    # error, preventing silent overwrites of an existing published release.
    - name: Create GitHub Release and upload binaries
      if: steps.version.outputs.is_release == 'true'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ steps.version.outputs.version }}
        name: v${{ steps.version.outputs.version }}
        fail_on_unmatched_files: true
        files: |
          target/release/botwork-launcher
          target/release/botwork-tools

  # ─────────────────────────────────────────────────────────────────────────
  # Job B — bump VERSION to the next minor -dev after a Release is published
  # Triggered by the "release published" webhook so it only fires for real
  # releases (not drafts, not pre-releases unless promoted to released).
  # ─────────────────────────────────────────────────────────────────────────
  bump:
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    permissions:
      contents: write   # commit and push VERSION bump to main

    steps:
    - uses: actions/checkout@v4
      with:
        ref: main
        # Use the default GITHUB_TOKEN; the resulting push triggers CI but
        # the new VERSION will end in "-dev" so Job A's publish guard treats
        # it as a no-op — no infinite loop.
        token: ${{ secrets.GITHUB_TOKEN }}

    # ── Derive released version from the tag (strip leading "v") ─────────────
    - name: Compute next minor dev version
      id: bump
      run: |
        set -euo pipefail
        tag="${{ github.event.release.tag_name }}"
        # Strip leading "v"
        version="${tag#v}"
        # Strip any pre-release/build-metadata suffix (e.g. "-rc1") so that
        # only the core MAJOR.MINOR.PATCH components remain for arithmetic.
        version="${version%%-*}"
        IFS='.' read -r major minor patch <<< "${version}"
        next_minor=$(( minor + 1 ))
        next_version="${major}.${next_minor}.0-dev"
        echo "next_version=${next_version}" >> "$GITHUB_OUTPUT"
        echo "Released: ${version} → next dev: ${next_version}"

    # ── Write bumped VERSION and push to main ────────────────────────────────
    - name: Bump VERSION and commit
      run: |
        set -euo pipefail
        echo "${{ steps.bump.outputs.next_version }}" > VERSION
        git config user.name  "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add VERSION
        git commit -m "chore: bump VERSION to ${{ steps.bump.outputs.next_version }} [skip ci]"
        git push origin main
